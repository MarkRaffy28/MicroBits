{
  "name": "microbits",
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -c \"blue,green\" \"node backend/server.js\" \"cross-env PORT=3000 npm start --prefix frontend\"",
    "t": "cd backend && node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "json-server": "^0.17.4"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "firebase": "^12.10.0",
    "sass": "^1.98.0"
  }
}
